/** @jsx jsx */
import { Flex } from "@theme-ui/components";
import { jsx } from "@theme-ui/core";

// markup
const IndexPage = () => {
  return <Flex sx={{ flexDirection: "column" }}>data</Flex>;
};

export default IndexPage;
